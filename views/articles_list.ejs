<!DOCTYPE html>
<html>
<head>
  <title>Articles in <%= category %></title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">

  <style>
  html, body {
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
  }
  .article {
    background-color: #E9E9E9;
    padding: 10px;
    margin: 10px;
  }
  .img-fit {
    width: calc(100% + 20px);
    margin-left: -10px;
    margin-top: -10px;
    margin-bottom: 10px;
  }
  </style>

  <meta name="viewport" content="width=device-width, initial-scale=1.0">

</head>
<body>

  <div class="container">
    <div class="row">
      <div class="col-lg-4 col-sm-2"></div>
      <div class="col-lg-4 col-sm-8 col-xs-12">
        <% if( articles.length == 0 ) { %>
        <h1>No articles found in <%= category %></h1>
        <% } else { %>  
        <% for (var i = 0; i < articles.length; i++) { %>
        <div class="row article">
          <% if( null != articles[i].headerImg ) { %>
          <img class="img-fit" src="https://s3.amazonaws.com/clarionimgs/<%= articles[i].id %>-<%= articles[i].headerImg %>" alt="<%= articles[i].urlTitle %>">
          <% } else { %>  
          <img class="img-fit" src="http://theccat.com/wp-content/themes/gonzo/images/no-image-featured-image.png">
          <% } %>
          <div class="col-xs-8">
            <span>
              <b>
                <a target="_blank" href="<%= urlBase %>/article/<%= articles[i].urlCategory %>/<%= articles[i].urlTitle %>"><%= articles[i].title %></a>
              </b>
            </span><br>
            <span><%= articles[i].datePublished %></span>
          </div>
          <div class="col-xs-4">
            <span><%= articles[i].author %></span>
          </div>
        </div><br>
        <% } %>
        <% } %>
      </div>
    </div>
    <div class="col-lg-4 col-sm-2"></div>
  </div>
</div>

</body>
</html>